import { styled } from '..'

export const Container = styled('div', {
  backgroundColor: '$backgroundColor',
  height: '100vh',
  width: '100vw',

  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
})
